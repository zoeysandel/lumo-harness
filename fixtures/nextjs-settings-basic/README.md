# Next.js Settings Fixture

Small app fixture for testing whether Lumo helps Codex build a scoped settings feature.

Rules for the target task:

- Keep settings state local-only.
- Do not add auth, database, external APIs, or new dependencies.
- Reuse the existing page/card/button styling patterns.
- Verify with `npm run build`.

