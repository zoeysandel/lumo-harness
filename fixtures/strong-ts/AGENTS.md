# Fixture Agent Rules

- Keep changes small.
- Run `npm run typecheck` and `npm test` for behavior changes.
- Pause before provider I/O, secrets, privacy, env, auth, migrations, or external side effects.
