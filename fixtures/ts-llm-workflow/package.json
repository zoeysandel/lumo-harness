{
  "name": "ts-llm-workflow-fixture",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/verify.mjs",
    "test": "node scripts/verify.mjs"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}
